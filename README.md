# searchallvisio
this is a powershell script that searches all visio files within a directory for a given string. Search results will be printed to a searchresults.txt file in whichever directory the script is located. Also, its not perfect. It will likely pull a couple false positives off the backround xml extraction but I have never seen it miss a positive match through extensive testing.
