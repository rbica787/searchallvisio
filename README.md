# searchallvisio
this is a powershell script that searches all visio files within a directory for a given string. Just change the 3 parameters at the top of the file before running the script. Search results will be printed to a searchresults.txt file in whichever directory the script is located.
